# Neptune Data Website

Built with React + Vite. Deploy to Cloudflare Pages.

## Local development

```bash
npm install
npm run dev
```

## Deploy to Cloudflare Pages

1. Push this folder to a GitHub repo
2. Go to Cloudflare Pages → Create a project → Connect to Git
3. Build settings:
   - Framework preset: Vite
   - Build command: `npm run build`
   - Output directory: `dist`
4. Add custom domain: neptunedata.ai

## Contact email
Update `sanchit@neptunedata.ai` in `src/App.jsx` if needed.
